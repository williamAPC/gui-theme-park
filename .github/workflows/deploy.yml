name: Deploy
run-name: ${{ github.actor }} is deploying  ${{ github.repository }} ${{ github.ref }} 🚀
on:
  push:
    branches:
    - cja-deploy
    
# on:
#  workflow_dispatch:
#    inputs:
#      environment:
#        description: 'Environnement de destination'
#        required: true
#        default: 'dev'
#        type: choice
#        options:
#        - dev
#        - prod
        
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: setup AWS credntials
        uses: aws-actions/configure-aws-credentials@v4
        with: 
          aws-region: eu-west-3
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      - name: setup kubeconfig
        run: |
          aws s3 ls
          
